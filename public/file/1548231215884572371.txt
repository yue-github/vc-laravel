							学习与问题总结

1.flux与redux的区别-》flux只提出了单项数据流，而redux在这基础上提出了1.唯一数据源2.状态只读操作3.状态完成只能通过纯函数完成。
---------------------------------------------------
2.vue中的依赖注入与react依赖注入类似；
---------------------------------------------------
3.vue属性传值与react传值，父向子，子向父，getChild
 ------------------------------------------------------
4.vue中的ref与react中的ref。
------------------------------------
5.react-router中渲染方式，render只可以渲染无状态组件，component可以渲染有无状态组件
-------------------------------------------------------
6.路由关联->let Li=(props)=>{
	return <li>
		<link to="/">matchs.children</link>

</li>
}
return <ul><Li></Li></ul>
----------------------- 
7.1 if ($users->first()) {//若无数据，打印出来为null
2     //
3  } 
4 if (!$users->isEmpty()) {
5     //
6  } 
7 if ($users->count()) {//若无数据，打印出来为0
8     //
9  }
laravel中判断数据库查询出的数据是否为空